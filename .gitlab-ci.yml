image: node:latest

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - docker build -t frontend .
  artifacts:
    paths:
      - node_modules/
      - dist/

test:
  stage: test
  script:
    - docker run frontend npm run dev
